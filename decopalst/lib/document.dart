import 'package:flutter/material.dart';

class docsex extends StatefulWidget {
  const docsex({super.key});

  @override
  State<docsex> createState() => _docsexState();
}

class _docsexState extends State<docsex> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text("data")
        ],
      ),
    );
  }
}